import { LandingPageTemplate } from 'components/template/LandingPageTemplate'
import { NOT_FOUND } from 'localization/NotFoundPage'

export const NotFoundPage = () => {
	return <LandingPageTemplate loc={NOT_FOUND} />
}
