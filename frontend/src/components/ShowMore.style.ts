import styled from 'styled-components'

export const ShowMoreStyle = styled.div`
	display: flex;
	flex-direction: column;
	gap: 4px;
	width: 100%;

	button {
		text-align: left;

		&:before {
			align-self: center;
		}
	}
`
